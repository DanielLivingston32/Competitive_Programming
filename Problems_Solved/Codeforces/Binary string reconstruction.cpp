#include <iostream>
#include <string>

using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        string input;
        cin >> input;
        int n;
        cin >> n;
        string result = "";
        for (int i = 0; i < input.size(); i++)
    }
}