#include <iostream>
#include <string>

using namespace std;


int main()
{
    int n;
    cin >> n;
    string s;
    cin >> s;
    bool *visited = new bool[n];
    int count = 0;
    int window_size = 1;
    int left=0,right=window_size;
    while()
    
    
}